import 'package:flutter/material.dart';

class OrderContet extends StatelessWidget {
  const OrderContet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('OrderContet Screen Content'),
    );
  }
}
